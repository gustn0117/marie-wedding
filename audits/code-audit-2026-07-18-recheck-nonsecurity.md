| 심각도 | 파일:라인 | 재현 시나리오 | 원인 | 권장 수정방향(설명만, 코드변경 금지) | 공수 |
|---|---|---|---|---|---|
| 높음 · 배포/일관성 | `scripts/apply-release-migrations.sh:29-57`<br>`README.md:96-111`<br>`supabase/migrations/20260714000300_password_admin_service_operations.sql:125-177`<br>`supabase/migrations/20260718000100_withdraw_purge_resumes_and_name_reset.sql:85-140` | 문서화된 기존 DB 절차대로 `db:release:pre-app`을 다시 실행하면 7월 18일 migration 네 개는 적용하지 않으면서 7월 14일의 구버전 `reactivate_profile_clean`을 `CREATE OR REPLACE`한다. 이후 탈퇴 사용자가 OAuth로 재가입하면 최신 함수가 비우던 `contact_name`이 남고 이력서 정리도 빠진다. 현재 운영 DB 함수는 최신이지만 공식 release 명령 1회로 다시 회귀한다. | migration ledger 없이 일부 파일을 매번 재실행하며 최신 migration을 목록에 포함하지 않는다. bootstrap의 최신 배열은 기존 DB release 경로를 보호하지 못하고 webhook 배포에도 DB migration 단계가 없다. | 적용 이력을 저장하는 단일 migration runner로 통합하고, 이미 적용된 최신 함수를 과거 파일이 덮어쓰지 못하도록 순서를 검증한다. 배포 전후 함수 본문·필수 인덱스 checksum을 확인한다. | 중 |
| 높음 · 재해복구 | `supabase/schema.sql:28-49`<br>`src/shared/constants/profileSelect.ts:7-8`<br>`src/app/page.tsx:77-86`<br>`scripts/bootstrap-database.sh:349-360` | fresh/DR DB를 bootstrap한 뒤 디렉터리·프로필 조회를 실행하면 앱이 요구하는 `profiles.featured_at`, `featured_order`가 없어 PostgREST `42703`이 발생한다. 디렉터리는 빈 목록 또는 404로 오인되고 추천 프로필은 사라진다. 운영 DB에는 두 컬럼이 실제 존재하지만 저장소의 schema와 migration 어디에도 profiles용 `ADD COLUMN`이 없다. 신규 DB 한 개부터 운영과 다른 결과가 난다. | 기준 schema에는 두 컬럼이 없고, featured migration은 `jobs`에만 추가한다. 타입·공개 select·관리자 화면은 운영 DB에 수동으로 존재하는 컬럼을 전제로 한다. bootstrap 최종 검증도 테이블 존재만 보고 필수 컬럼은 검사하지 않는다. | 운영 DB의 실제 컬럼 정의를 명시적 migration으로 승격하고 bootstrap 검증에 필수 컬럼·타입·기본값을 포함한다. 빈 DB에서 주요 SSR query smoke test를 실행한다. | 소~중 |
| 높음 · 이력서 스냅샷 | `supabase/migrations/20260715000100_resume_system.sql:616-654`<br>`src/features/resumes/lib/mapper.ts:153-158`<br>`src/lib/storage-orphan-cleanup.ts:211-218` | fresh/DR DB에서 PDF 첨부가 있는 이력서로 지원하면 `application_resume_snapshots.snapshot`에 `attachments`가 복제되지 않는다. 고용주는 제출본 첨부를 즉시 볼 수 없고, 원본 이력서에서 파일을 교체하면 snapshot 참조가 없어 고아정리 대상이 된다. 지원 1만 건이면 1만 제출본이 동일하게 불완전해진다. 운영 함수에는 별도 hotfix가 있으나 canonical migration에는 없다. | snapshot JSON 생성 목록이 `links`에서 끝나며 앱 mapper와 고아정리는 `snapshot.attachments`가 존재한다고 가정한다. 운영 함수와 저장소 migration이 서로 다르다. | 운영 함수의 최신 정의를 새 migration으로 고정하고, 이력서의 모든 사용자 필드가 제출 snapshot에 복제되는지 DB 통합 테스트로 검증한다. | 소~중 |
| 높음 · 배포 레이스 | 운영 `auto-deployer:/app/webhook.py:45-78`<br>`webhook-deploy.sh:34-76,97-181` | push 두 건 또는 수동 배포와 자동 배포가 겹치면 두 실행이 같은 활성 색을 읽고 같은 idle 컨테이너를 동시에 build/recreate한다. A가 nginx를 새 색으로 전환한 직후 B가 같은 컨테이너를 다시 만들거나 실패 정리하면 현재 upstream이 사라져 502가 된다. 연속 push 두 번부터 재현 가능하며 배포 빈도가 증가할수록 확률이 커진다. | 저장소별 lock/queue가 없고 활성 색 판정부터 swap·drain·cleanup까지 원자적이지 않다. 각 요청이 독립 thread를 생성한다. | 배포 전체 구간에 host-level 단일 lock 또는 저장소별 큐를 두고, 대기 중 commit은 최신 하나로 합친다. swap 이후 cleanup은 현재 upstream을 다시 확인한 뒤 수행한다. | 소~중 |
| 높음 · 계정 복구 성장성 | `src/lib/authUsers.ts:8-20`<br>`src/app/api/password-reset/send/route.ts:40-45`<br>`src/app/api/password-reset/confirm/route.ts:118-129` | `auth.users`가 20,001명이고 대상이 21번째 페이지 이후에 있으면 재설정 메일은 조용히 누락되고, 정상 OTP를 입력해도 “가입 정보를 찾을 수 없음”이 반환된다. 존재하지 않는 이메일 조회 한 번도 최대 20회의 Auth API와 2만 사용자 객체 순회를 일으킨다. 현재 Auth는 25명이라 잠복 상태다. | 1,000명 단위 `listUsers` 전수 탐색을 20페이지에서 강제 종료한다. 운영 Auth DB에는 `(instance_id, lower(email))` 인덱스가 있지만 이 경로는 사용하지 않는다. | 지원되는 exact-email 조회 또는 제한된 server-only indexed RPC로 교체하고 계정 존재 여부를 숨기는 현재 응답 의미는 유지한다. | 중 |
| 높음 · 소셜 로그인 일관성 | `src/app/auth/naver/callback/route.ts:148-199,202-237`<br>`src/app/api/onboarding/route.ts:75-117`<br>`src/app/api/account/withdraw/route.ts:74-84` | 신규 Naver 가입에서 Auth `createUser`는 성공했지만 profile INSERT가 timeout·제약 오류로 실패하면 오류를 확인하지 않고 세션을 발급한다. 온보딩 UPDATE 0행도 성공 응답이며 다음 로그인은 동일 이메일 Auth 생성 충돌로 반복 실패한다. 가입 1만 건에서 두 시스템 사이 실패율을 0.1%로 가정하면 약 10개의 수동 복구 계정이 생긴다. | 외부 Auth 생성과 profile 생성 사이에 성공 확인·보상·멱등 복구가 없다. 이메일 가입 경로의 보상 삭제가 Naver 경로에는 없고 no-profile 탈퇴도 쿠키만 지운다. | profile 생성 성공을 필수 확인하고 Naver 식별자 기반 멱등 재개 또는 실패 시 Auth 생성 보상을 수행한다. 온보딩 UPDATE도 영향 행 존재를 확인한다. | 중 |
| 높음 · 레이스/비용증폭 | `supabase/migrations/20260524_platform_systems.sql:226-269`<br>`src/features/community/components/LikeButton.tsx:23-48`<br>`supabase/migrations/2026-06-16-refocus-jobs-platform.sql:99-167` | 서로 다른 두 사용자가 같은 글에 동시에 첫 좋아요를 넣고 양쪽 COUNT가 상대 미커밋 행을 보기 전에 실행되면 `post_likes=2`인데 마지막 `posts.like_count=1`이다. RPC commit 뒤 응답 지연으로 UI가 재시도하면 toggle이 반대로 실행돼 좋아요가 취소될 수도 있다. 좋아요 10만 회 누적 시 전체 COUNT 작업은 약 50억 index-entry 검사, full-post 감사 사본은 10만 건에 이른다. 현재 posts 2행·likes 0행이다. | desired state가 아닌 toggle이고, 매 클릭마다 전체 `COUNT(*)` 후 hot row에 상수를 덮어쓴다. 단일 사용자 unique와 UI loading은 서로 다른 사용자 경쟁·응답 유실을 막지 못한다. `like_count`만 바뀌어도 감사 trigger가 게시글 OLD/NEW 전체를 복제한다. | client-generated operation ID와 desired-state RPC를 사용하고 원자적 증감 또는 게시물 단위 직렬화를 적용한다. like_count-only 변경은 full-row 감사 대상에서 분리한다. | 중 |
| 높음 · 메시징 조회 | `src/features/messages/services/conversationSummaries.ts:25-74`<br>`src/app/(main)/mypage/messages/[id]/page.tsx:45-79`<br>`src/features/messages/components/MessageThread.tsx:48-69` | 대화 A에 다른 대화의 마지막 메시지보다 최신인 메시지 300개가 있으면 글로벌 `LIMIT 300`을 A가 독점해 다른 대화의 `lastBody=null`이 된다. 51번째 대화는 목록에서 사라진다. 메시지 10만 건 대화는 전 이력을 RSC와 DOM에 올리고, 50개 대화마다 미읽음 2만 건이면 100만 행을 Node로 전송해 JS에서 센다. 375px 모바일과 4-worker 서버가 함께 정체된다. 현재 messages/conversations는 0행이다. | 대화별 최신 한 건이 아니라 전체 최신 300건을 조회하며, unread는 DB 집계가 아니고 상세 pagination도 없다. 실제 DB는 `(conversation_id,created_at)` 인덱스만 있고 unread partial index가 없다. EXPLAIN은 bitmap scan 후 global sort였다. | 대화별 최신 한 행과 unread COUNT를 DB에서 집계하고 목록·상세를 keyset pagination한다. 큰 thread는 가상화하고 unread 조건에 맞는 partial index를 검토한다. | 대 |
| 높음 · 지원자 목록 | `src/features/applications/components/JobApplicationBox.tsx:114-149,403-506`<br>`src/app/api/applications/hiring/route.ts:121-138` | 공고당 지원 10,000건이면 상세 진입 한 번에 100행짜리 100개 페이지와 EOF 확인용 한 페이지, 총 101개 요청을 직렬 실행하고 10,000개 카드를 DOM에 올린다. 51번째 요청에서 실패하면 5,000건만 받은 상태를 총건수·상태별 수로 표시한다. 담당자 50명 동시 접속이면 약 5,050개의 인증/API/DB 요청이 발생한다. 현재 applications는 1행이다. | 서버 cursor를 클라이언트 `do…while`이 자동으로 끝까지 소진한다. 실제 DB에는 활성 `(job_id)` 인덱스만 있고 정렬용 `(job_id,created_at,id)` 복합 partial index가 없다. | 사용자 동작 기반 더보기, 서버 상태별 집계, 목록 가상화로 분리하고 cursor 정렬에 맞는 복합 인덱스를 추가한다. | 중~대 |
| 높음 · 외부 메일 멱등성 | `src/app/api/applications/create/route.ts:153-180`<br>`supabase/migrations/20260715000100_resume_system.sql:484-507`<br>`src/features/notifications/lib/applicationNotify.ts:10-42`<br>`src/features/applications/components/JobApplicationBox.tsx:290-327` | 지원 DB commit 뒤 SMTP가 지연돼 클라이언트 10초 timeout이 나면 같은 `applicationId`로 재시도한다. DB는 기존 지원서를 replay하지만 route는 고용주 메일을 다시 발송한다. 지원 1만 건의 1%가 재시도돼도 중복 메일 약 100건이며 동시 100건이면 SMTP socket과 Node worker가 장시간 점유된다. | DB 멱등성에 외부 side effect의 최초 생성/replay 구분이 없고 SMTP를 HTTP 응답 전에 `await`한다. UI timeout은 underlying 요청을 즉시 중단하지 않는다. | application/event 고유키를 가진 durable outbox를 DB 트랜잭션에서 생성하고 즉시 응답한다. 별도 worker가 provider idempotency와 재시도를 담당한다. | 중~대 |
| 높음 · 스토리지 정리 | `src/lib/storage-orphan-cleanup.ts:17-20,123-175,223-269,286-312`<br>`src/app/api/admin/storage-cleanup/route.ts:9-12,61-70` | 참조 테이블 하나가 정확히 100,000행이면 마지막 500행을 읽고도 EOF를 확인하지 못한 채 상한 예외가 발생해 이후 정리가 계속 실패한다. bucket은 100,001개부터 실패한다. 평균 20KB rich-text 5만 행이면 한 테이블 원문만 약 1GB이며, 4-worker에 요청이 분산되면 프로세스별 lock 때문에 최대 네 전수 작업이 동시에 돈다. 현재 앱 테이블 최대 3행·bucket 최대 117개다. | 8개 테이블과 모든 bucket 객체를 메모리에 전수 적재하고 hard cap을 fatal로 처리한다. `activeCleanup`은 worker 로컬이라 cluster/blue-green 간 공유되지 않는다. | cursor/watermark 기반 증분 인벤토리와 재개 가능한 background job으로 전환하고 DB advisory lock 등 분산 실행 잠금을 둔다. | 대 |
| 높음 · 장애 표현 | `src/app/(main)/jobs/[id]/page.tsx:23-31`<br>`src/app/(main)/community/[id]/page.tsx:23-34`<br>`src/app/(main)/events/[id]/page.tsx:22-30`<br>`src/app/(main)/jobs/page.tsx:108-113`<br>`src/app/(main)/community/page.tsx:59-67`<br>`src/app/(main)/events/page.tsx:72-73` | DB 포화 중 기존 행 조회가 PostgREST HTTP 5xx·statement 오류를 반환하면 상세는 `data=null`만 보고 404, 목록은 빈 배열·0건으로 표시한다. 활성행 1만·동접 수백에서 실제 장애가 “삭제된 콘텐츠” 또는 “빈 사이트”로 위장되어 사용자는 재시도하지 못하고 운영 탐지도 늦어진다. | Supabase `error`를 확인하지 않는다. fetch abort/throw는 500이 되지만 정상 HTTP 응답 안의 DB 오류는 throw되지 않으므로 기존 방어가 적용되지 않는다. | 실제 not-found 코드만 404로 분류하고 timeout·DB 오류는 throw하거나 503으로 표시한다. 오류율·false-404 지표를 별도로 수집한다. | 소~중 |
| 높음 · OG 렌더 비용 | `next.config.mjs:13-24`<br>`src/lib/supabase/server-query.ts:8-23`<br>`src/app/(main)/directory/[id]/opengraph-image.tsx:15-58`<br>`src/app/(main)/jobs/[id]/opengraph-image.tsx:17-61` | 동일 공유 URL의 OG 이미지를 반복 요청해도 응답이 `no-cache, no-store`, `cf-cache-status:DYNAMIC`이며 매번 DB 조회와 PNG 렌더링을 한다. 운영 실측은 약 0.6~1.1초였다. 바이럴 링크가 10req/s 이상으로 unfurl되면 4개 Node worker의 CPU·DB 요청이 OG 생성에 묶여 일반 페이지까지 지연될 수 있다. | 전역 no-store header가 동적 OG까지 덮고 server query도 no-store다. nginx/Cloudflare에 OG 전용 캐시가 없다. 이번 502 crash 수정의 fallback 자체는 정상 동작함을 확인했다. | OG 경로에 route-specific CDN cache와 stale-while-revalidate를 적용하거나 데이터 변경 시 미리 생성한다. 존재하지 않는 ID의 fallback도 짧은 negative cache를 둔다. | 소~중 |
| 중간 · 메시지 멱등성 | `src/features/messages/components/MessageThread.tsx:30-44`<br>`src/features/messages/services/messageService.ts:25-37`<br>`src/app/api/messages/send/route.ts:11-35`<br>`supabase/migrations/2026-05-25-platform-boost-3.sql:130-168` | message와 notification이 commit된 뒤 응답만 유실되면 재전송으로 동일 내용이 두 번 생성된다. 10만 발송의 1%가 재시도되면 약 1,000쌍이 중복된다. 또한 오래 시작한 트랜잭션 A가 정지한 사이 B가 새 시각을 commit하고 A가 더 오래된 transaction `NOW()`로 후커밋하면 `last_message_at`이 뒤로 간다. | client message UUID·unique idempotency가 없고 route가 request abort를 RPC에 전달하지 않는다. `last_message_at=NOW()`도 기존 값과 최대값 비교 없이 덮어쓴다. | client-generated message ID를 저장하고 replay 시 기존 결과를 반환한다. 대화 시각은 메시지 insert trigger 또는 `GREATEST` 의미로 갱신한다. | 중 |
| 중간 · KPI 정확성 | `src/app/(main)/mypage/dashboard/page.tsx:34-69,77-86,96-130`<br>`src/app/(main)/mypage/page.tsx:27-57,229-293` | 지원 101건 중 최신 100건은 승인, 가장 오래된 1건은 pending이면 대시보드는 총100·pending0·승인율100%로 표시하지만 실제는 101·1·약99%다. 마이페이지 공고·게시글·지원도 51건부터 탭과 수치가 50에서 멈춘다. | `range(0,49/99)`로 잘린 배열 길이와 filter 결과를 전체 KPI로 사용하고 목록 pagination이 없다. | KPI는 DB conditional aggregate/count로 계산하고 최근 목록과 분리한다. 각 탭은 cursor pagination을 제공한다. | 중 |
| 중간 · 중복 인증 조회 | `src/shared/hooks/useAuth.ts:26-108`<br>`src/app/(main)/community/[id]/page.tsx:90-96,140-141,164-169`<br>`src/app/api/profile/me/route.ts:15-46` | 로그인 사용자가 게시글 상세를 한 번 열면 네 컴포넌트의 독립 `useAuth`가 `/api/profile/me`를 네 번 호출한다. 동접 100이면 약 400회, 1,000이면 약 4,000회의 API 인증과 profile 조회가 발생한다. | hook마다 별도 client/state/effect를 만들며 context·SWR dedupe가 없다. profile route도 verified-profile 확인 뒤 full profile을 다시 조회한다. | layout 단일 AuthProvider 또는 요청 dedupe를 사용하고 SSR에서 이미 확인한 viewer 정보를 하위 컴포넌트에 전달한다. | 중 |
| 중간 · 지역 필터 | `src/app/(main)/jobs/page.tsx:61-73`<br>`src/shared/constants/regions.ts:37,49,52,153,158,165` | `region=danyang`인 단양 공고가 경기→안양(`anyang`) 필터에 포함된다. `osan⊂seosan`, `yangju⊂namyangju`도 동일하다. 현재 jobs 3행에서 실제 EXPLAIN은 약 0.1ms지만 `Seq Scan+Sort`였으며 활성 10만·동접100에서는 wildcard 조건과 exact count가 반복 전수검사된다. | CSV 값을 leading-wildcard `ILIKE '%token%'`로 비교해 문자열 충돌이 나고 실제 region B-tree를 사용하지 못한다. | 토큰 경계를 정확히 비교하고 근본적으로 `text[]` 또는 지역 조인 테이블로 정규화해 배열 연산과 적합한 인덱스를 사용한다. | 중~대 |
| 중간 · 급여 검증 정합성 | `src/features/jobs/components/JobForm.tsx:446-473`<br>`src/features/jobs/components/JobsPageContent.tsx:640-647`<br>`src/app/(main)/jobs/page.tsx:81-88` | 시급 15,000원 공고가 “월 400만원 이상” 결과에 포함된다. 일급·시급 공고 1,000개가 생기면 월급 필터가 광범위하게 오염된다. | 월·연은 만원, 일·시는 원으로 저장하면서 검색은 `salary_unit` 없이 raw `salary_min/max`만 비교한다. | 검색에서 급여 단위를 제한하거나 모든 급여를 공통 기간·통화 단위의 정규화 값으로 변환하고 기존 데이터도 이관한다. | 중 |
| 중간 · 통계 캐시 | `src/app/(main)/stats/page.tsx:13,24-78` | 공개 프로필 5,001개부터 무순서 5,000행만 지역·업종 분포로 집계해 합계가 축소된다. count 또는 분포 query 하나가 실패해도 `0/[]`를 성공값으로 5분 cache한다. Docker replica 두 개 중 하나만 실패하면 새로고침마다 정상 수치와 0이 교차할 수 있다. | 전체 DB 집계 대신 서로 독립적인 제한 표본을 사용하고 query error를 확인하지 않은 채 성공 cache에 넣는다. | DB `GROUP BY` 집계/RPC로 전환하고 일부 query 오류는 전체 결과를 실패 처리한다. 캐시에는 성공 결과만 기록한다. | 중 |
| 중간 · 달력 레이스 | `src/features/availability/components/AvailabilityCalendar.tsx:43-58,66-99` | 7→8→9월을 빠르게 이동하고 7월 응답이 가장 늦게 도착하면 9월 헤더 아래 7월 slots가 표시된다. 이전 요청의 `finally`가 최신 요청 중에도 `busy=false`로 만들어 잘못된 월 상태를 클릭·덮어쓸 수 있다. | 월별 요청 generation·abort가 없고 모든 응답이 같은 `slots/busy` state를 갱신한다. | AbortController 또는 sequence token으로 최신 요청만 state를 갱신하게 하고 loading도 요청별로 관리한다. | 소 |
| 중간 · 이벤트 날짜 | `src/features/events/components/EventForm.tsx:86-139,258-297`<br>`src/app/api/admin/route.ts:513-534,570-589`<br>`src/app/(main)/events/page.tsx:33-70`<br>`supabase/schema.sql:116-125` | 시작 `2026-08-10`, 종료 `2026-08-01`도 저장되고 8월5일에는 “종료” 목록에 표시된다. 종료일이 없는 7월18일 단일일 이벤트는 다음날 기본/종료 양쪽에서 사라진다. KST 00:00~08:59에는 UTC 기반 `todayIso` 때문에 상태가 전날 기준으로 계산된다. | client/API/DB에 `start<=end` 제약이 없고 optional 날짜 조합 의미가 화면마다 다르다. `ended` 구현은 null 종료일의 지난 시작일을 포함하지 않는다. | KST 기준 DATE helper를 통일하고 DB CHECK와 서버 검증을 추가한다. 단일일·기간·상시 이벤트의 상태식을 명시적으로 분리한다. | 소~중 |
| 중간 · 이벤트 목록 | `src/app/(main)/events/page.tsx:66-73,121-238` | 진행 이벤트가 51개이면 헤더 count는 51이지만 첫 50개만 렌더되고 pagination·더보기가 없어 51번째부터 목록으로 접근할 수 없다. | exact count와 별개로 고정 `range(0,49)`만 조회한다. | keyset pagination 또는 더보기를 제공하고 count와 현재 로드 범위를 구분해 표시한다. | 소~중 |
| 중간 · 통합검색 정확성 | `src/app/(main)/search/page.tsx:31-77,93-104,117-183` | 제목 일치 채용이 11개면 화면은 “총 10건/채용 10”으로 표시한다. 같은 업체명 프로필이 51개면 임의 50개만 작성자 검색에 쓰며, 업체명으로 찾은 공고의 “더보기”는 제목 전용 검색으로 이동해 결과가 사라진다. | 각 도메인 preview 최대 10개의 배열 길이를 총계로 사용하고 작성자 profile을 50개로 자른다. 통합검색과 도메인 페이지의 검색 의미도 다르다. | 서버 검색 RPC에서 도메인별 count와 cursor를 함께 반환하고 더보기 URL도 동일한 검색 의미를 보존한다. | 중 |
| 중간 · 공고 재개 | `src/app/(main)/mypage/page.tsx:27-35`<br>`src/features/jobs/components/JobStatusMenu.tsx:10-40`<br>`src/app/api/jobs/status/route.ts:76-100`<br>`src/app/(main)/jobs/page.tsx:46-52` | 기한이 지난 closed 공고에서 “다시 모집”을 누르면 성공 toast와 `status=open`이 되지만 과거 deadline 때문에 목록·상세에서는 계속 마감이다. 30분 cron이 다시 closed로 되돌린다. | 상태 API가 deadline을 읽거나 갱신하지 않고 status만 변경하며 마이페이지 query에도 deadline이 없다. | 미래 deadline 설정 또는 deadline 제거와 reopen을 하나의 원자 작업으로 묶고 UI에서 새 마감일을 요구한다. | 소 |
| 중간 · 375px pagination | `src/features/jobs/components/JobsPageContent.tsx:550-587`<br>`src/app/globals.css:15,80-84`<br>`src/shared/components/Pagination.tsx:9-95` | 375px에서 실제 본문 폭은 343px다. 활성 공고 141개(8쪽)부터 pagination 최소폭 `10×32 + 9×4 = 356px`가 가로로 넘친다. 공고 201개부터 page 11에서도 버튼은 1~10만 있어 현재 페이지 표시와 직접 이동이 사라진다. | 항상 1~10 버튼을 렌더하는 별도 pagination이며 wrap·scroll·현재 페이지 window가 없다. 공용 ellipsis Pagination은 이미 있지만 이 화면이 사용하지 않는다. | 공용 windowed pagination을 사용하고 375px에서 버튼 수와 크기를 줄인다. | 소 |
| 중간 · 댓글 대량 렌더 | `src/features/community/services/community-service.ts:163-177`<br>`src/features/community/components/CommentSection.tsx:62-75,111-116,180-260` | 댓글 10,000개 게시글을 열면 전체 fetch 후 매 render마다 복사·정렬하고 10,000개 DOM을 만든다. 375px 모바일에서 장시간 정체하며, 서버 반환 상한이 있으면 ascending 조회의 오래된 일부만 남아 최신 댓글이 조용히 사라질 수 있다. | app-level pagination이 없고 실제 DB에는 활성 `(post_id)` 인덱스만 있으며 chronological 복합 인덱스가 없다. | 최신 window와 keyset pagination, 목록 가상화를 적용하고 `(post_id,created_at,id)` 정렬 인덱스를 검토한다. | 중 |
| 낮음~중간 · 대화 생성 레이스 | `supabase/migrations/2026-05-25-platform-boost-3.sql:98-125`<br>`src/app/api/messages/start/route.ts:13-31` | 아직 대화가 없는 A와 B가 동시에 최초 대화 시작을 누르면 둘 다 SELECT에서 없음으로 판단하고 INSERT한다. unique `(participant_a,participant_b)` 때문에 한 요청은 generic 실패를 받는다. 사용자 두 명의 동시 동작부터 재현된다. | SELECT→INSERT가 원자적 upsert가 아니며 unique violation 때 기존 대화를 다시 읽지 않는다. | `ON CONFLICT` 의미의 원자 생성 후 기존 conversation을 반환한다. | 소 |
| 자기비평 | — | 보안·인가·RLS·권한·개인정보 노출 관련 발견은 사용자 요청에 따라 이 문서에서 전부 제외했다. 읽기 전용 제약으로 동시 쓰기·장애 주입·대량 브라우저 렌더는 실행하지 않았으며, 운영 행수·인덱스·함수·`EXPLAIN ANALYZE`와 코드 경로로 재현 조건을 검증했다. | — | — | — |
