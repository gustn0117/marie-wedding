import AdminProxyJobNewClient from './AdminProxyJobNewClient';

export const dynamic = 'force-dynamic';
export const metadata = { title: '대행 공고 등록 | 관리자' };

export default function AdminProxyJobNewPage() {
  return <AdminProxyJobNewClient />;
}
